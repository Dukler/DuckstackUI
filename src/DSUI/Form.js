import React from 'react';
import UI from "./UI";

export default class Form extends React.Component {
    constructor(props) {
        super(props);
        this.state = {};

      }

        render() {
            return (
                <UI />
            );
        }
  }